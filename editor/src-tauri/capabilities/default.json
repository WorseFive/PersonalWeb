{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local PersonalWeb editor window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
