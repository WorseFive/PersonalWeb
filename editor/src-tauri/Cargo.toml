[package]
name = "personalweb-editor"
version = "0.1.0"
description = "Local-first content editor for PersonalWeb"
authors = ["WorseFive"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
